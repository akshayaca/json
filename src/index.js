import React from 'react';
import ReactDOM from 'react-dom'; // Import your CSS file if you have one
import App from './App'; // Assuming your main component is named App

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
